import java.awt.*;
import java.time.LocalDate;

public class Opel extends ModernCar {
    public Opel(String modelName, int maxSpeed, Color color, LocalDate productionDate, int weight, int seats) {
        super(modelName, maxSpeed, color, productionDate, weight, seats);
    }
}